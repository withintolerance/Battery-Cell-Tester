# Optional. Defaults to localhost when unset — copy to .env.local only if the
# hub runs on a different host than the UI.
# NEXT_PUBLIC_HUB_URL=http://localhost:3001
# NEXT_PUBLIC_HUB_WS=ws://localhost:3001
